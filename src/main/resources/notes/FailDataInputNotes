PROBLEM: в классе InitialDataScannerSystemIn была следующая ошибка (ниже вывод на консоль работы программы):

jhk, Bmw, 123, 456, 0.2
Автомобиль jhk принят в гонку.
Введите следующий автомобиль, либо напечатайте esc и нажмите Enter для перехода на следующий этап.
jhk, Bmw, 0, 456, 0.2
Автомобиль jhk: ускорение должно быть больше 0: ошибка ввода.
Автомобиль jhk принят в гонку.
Введите следующий автомобиль, либо напечатайте esc и нажмите Enter для перехода на следующий этап.
esc

Process finished with exit code 0

CAUSE: сеттеры в Vehicle не пропускают нулевые значения, не записывают их в переменные. Поэтому когда я указал
0 в acceleration второго автомобиля, он не был пропущен, и там осталось значение из предыдущего автомобиля (123),
и неправильный автомобиль был записан как правильный.

SOLUTION: решается с помощью метода из Vehicle    setNullConstructorArguments() (обнулили аргументы конструктора, но минуя сеттеры
класса Vehicle, поскольку сеттеры сразу выдали бы сообщение об ошибке)

ANOTHER SOLUTION: сеттеры в Vehicle сделать не пустыми, а String, и сообщение об ошибке возвращать и принимать только
тогда, когда нужно. Но не прокатит: опять же, сеттеры не пропускают нулевые значения.